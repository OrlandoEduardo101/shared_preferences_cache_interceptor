name: shared_preferences_cache_interceptor
description: Package to implements cache using shared preferences to hasura_connect package
version: 1.0.0-dev.4
homepage: https://flutterando.com.br
repository: https://github.com/Flutterando/shared_preferences_cache_interceptor

environment:
  sdk: ">=2.12.0-0 <3.0.0"
  flutter: ">=1.17.0 <3.0.0"

dependencies:
  shared_preferences: ">=2.0.0 <3.0.0"
  hasura_cache_interceptor: ">=1.0.0-dev.5 <2.0.0"
  # hasura_cache_interceptor: 
  #   git: https://github.com/OrlandoEduardo101/hasura_cache_interceptor.git
      # resolved-ref: 9767f5f52a4549dd6278790da90841d7e550d43b
  flutter:
    sdk: flutter
  
dependency_overrides:
  platform: ^3.1.0
  # hasura_connect: ^4.0.0+2
  hasura_cache_interceptor: 
    git: https://github.com/OrlandoEduardo101/hasura_cache_interceptor.git

dev_dependencies:
  mocktail: ^0.3.0 
  # mockito: ^4.1.1
  flutter_test:
    sdk: flutter

flutter: